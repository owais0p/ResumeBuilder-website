import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Resume & Portfolio Builder",
  description: "Create professional resumes and portfolios in minutes with AI-powered suggestions, multiple templates, and PDF download.",
  keywords: ["resume builder", "portfolio builder", "AI resume", "professional resume", "PDF resume", "career"],
  authors: [{ name: "ResumeAI" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "AI Resume & Portfolio Builder",
    description: "Build professional resumes and portfolios with AI assistance",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "AI Resume & Portfolio Builder",
    description: "Build professional resumes and portfolios with AI assistance",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
